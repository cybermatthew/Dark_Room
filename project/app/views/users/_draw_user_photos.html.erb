<% if photos === nil || @userPhotos.empty? %>
	<p>No photos yet!</p>
<% else %>
	<% photos.each do |p| %>
		<a href="/photos/<%=p.id%>"><img src="<%= p.filename %>" class="thumbnail"></a>
	<% end %>
<% end %>